{% extends "base.twig" %}

{% block title %}Sign Up Page{% endblock %}

{% block body %}
    <section class="hero is-fullheight is-bold is-info">
        <div class="hero-body">
            <div class="container has-text-centered">
                <div class="column is-4 is-offset-4">
                    <h3 class="title has-text-white">Registra't</h3>
                    <hr class="login-hr">
                    <p class="subtitle has-text-white">Forma part de la propera superpotència!</p>
                    <div class="box">
                        <form method="POST">
                            <div class="field">
                                <div class="control">
                                    <input class="input is-large" type="email" name="email" placeholder="Your Email" autofocus="">
                                </div>
                            </div>

                            <div class="field">
                                <div class="control">
                                    <input class="input is-large" type="password" name="password" placeholder="Your Password">
                                </div>
                            </div>

                            <hr class="is-divider is-black">

                            <div class="field">
                                <div class="control">
                                    <input class="input is-large has-text-grey-light" type="date" name="birthday" placeholder="Your Birthday" autofocus="">
                                </div>
                            </div>
                            <button class="button is-block is-info is-large is-fullwidth" formaction="sign-up">
                                Registra't
                                <i class="fa fa-sign-in" aria-hidden="true"></i>
                            </button>
                        </form>
                    </div>
                    <p class="has-text-black">
                        <a href="../">Sign In</a>
                    </p>
                </div>
            </div>
        </div>
    </section>
{% endblock %}